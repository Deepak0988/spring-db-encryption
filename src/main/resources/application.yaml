spring:
  datasource:
    url: jdbc:mysql://localhost:3306/local
    username: root
    password: A!rforc3
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      springframework: DEBUG
